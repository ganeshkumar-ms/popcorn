version: 0.2

phases:
  install:
    commands:
      - echo "Install phase started"

  pre_build:
    commands:
      - echo "Pre-build phase started"

  build:
    commands:
      - echo "Build phase started"
      - echo "Preparing application artifact"

  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'